#include <stdio.h>
#include <stddef.h>

<% headers.to_a.each do |header| -%>
#include <<%= header %>>
<% end -%>

int conftest_const = (int)(offsetof(<%= struct_type %>, <%= field %>));

int main(){
  printf("%d\n", conftest_const);
  return 0;
}
