<% headers.to_a.each do |header| -%>
#include <<%= header %>>
<% end %>
int check_for_function(){
  void ((*volatile p)());
  p = (void ((*)()))<%= function %>;
  return 0;
}

int main(){
  return 0;
}
